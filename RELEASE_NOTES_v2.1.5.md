# Nuzi UI v2.1.5

## Fixes

- Fixed stock target-of-target frames sometimes showing the boss HP bar instead of the boss target's own HP.
- Fixed raid sign and marker state bleeding onto unrelated target-of-target frames after switching targets.
- Normalized watchtarget and target-of-target stock frame bindings so secondary target data refreshes against the correct live unit.
